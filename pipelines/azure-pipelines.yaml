name: $(MODULE_NAME)-$(date:yyyyMMdd)-$(rev:.r)
parameters:
  - name: publish_module
    type: boolean
    default: false
trigger:
  branches:
    include:
      - main
resources:
  repositories:
    - repository: self
    - repository: templates
      type: github
      name: Think-Cube/azure-devops-pipeline-templates
      ref: refs/heads/main
      endpoint: github.com
variables:
  - name: VM_IMAGE
    value: ubuntu-latest
  - name: MODULE_NAME
    value: 'terraform-azure-route-table'
stages:
  - template: azure-devops/templates/pipelines/validate_and_publish_tf_module.yml@templates
    parameters:
      publishModule: ${{ parameters.publish_module }}
      terraformVersion: 'latest'
      tfsecVersion: 'v1.26.0'
      allowTelemetryCollection: false
      GithubRepo: 'Think-Cube/terraform-azure-route-table'
      ModuleName: 'terraform-azure-route-table'
      ModuleDescription: 'Terraform module for Azure Route Table'